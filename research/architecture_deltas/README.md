# Architecture Deltas

Store one lightweight closeout artifact per architecture-affecting branch.

Use `research/ARCHITECTURE_DELTA_TEMPLATE.md` as the format source.
If a branch has no architecture state change, do not add a delta file; record `no architecture state change` in branch closeout.
